Пиши код в соответствии с руководством по стилю от Microsoft.

# Структуры данных
Пиши summary для всех конструкторов, не являющихся приватными(private) summary дложно быть написано по следующему шаблону `Инициализирует новый экземпляр <see cref="НазваниеКласса"/>`.

# Поля
приватные поля должны начинатся с символа нижнего подчеркивания `_`.